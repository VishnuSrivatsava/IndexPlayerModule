// Minimal service file for TizenBrew
console.log("IndexPlayer service started");
window.addEventListener('load', function() {
    document.getElementById('player').innerHTML = ''; // Clear player area
    // Optional: Initialize app logic here if needed
});